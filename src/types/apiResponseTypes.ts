export type MeType = {
    first_name: string | null,
    last_name: string | null,
    middle_name: string | null,
    gender: string | null,
    birth_date: string | null,
    email: string,
    role: string
}