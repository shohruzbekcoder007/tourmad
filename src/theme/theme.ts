export type ThemeType = typeof light;

export const light = {
  main_bg: "#FAFBFC",
  neutrals: '#ffffff'
}
export const dark: ThemeType = {
    main_bg: "#FAFBFC",
    neutrals: '#ffffff'
}

const theme = light;
export default theme;