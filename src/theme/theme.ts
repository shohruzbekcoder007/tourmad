export type ThemeType = typeof light;

export const light = {
  primary: "#f45511",
  text: "#000",
  background: "#fff",
}
export const dark: ThemeType = {
    primary: "#f45511",
    text: "#000",
    background: "#fff",
}

const theme = light;
export default theme;