import styled from "styled-components";

export const ServicesLinks = styled.div`
    width: 100%;
    display: flex;
    overflow-x: scroll;
    &::-webkit-scrollbar{
        display: none;
    }
    padding-left: 10px;
    align-items: center;
    justify-content: center;
    gap: 15px;
`